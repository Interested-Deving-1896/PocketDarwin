--- a/osfmk/mach/mig.h
+++ b/osfmk/mach/mig.h
@@ -53,6 +53,15 @@
 #define __MigKernelSpecificCode 1
 #define _MIG_KERNEL_SPECIFIC_CODE_ 1
 
+/* Compatibility shim for older trees built with newer MIG outputs. */
+#ifndef MACH_MSG_DESTROY_FROM_KERNEL_COMPAT
+#define MACH_MSG_DESTROY_FROM_KERNEL_COMPAT 1
+static __inline__ void mach_msg_destroy_from_kernel(mach_msg_header_t *msg)
+{
+	(void)msg;
+}
+#endif
+
 #elif !defined(__MigTypeCheck)
 
 #if defined(TypeCheck)
