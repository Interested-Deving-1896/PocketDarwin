diff --git a/SETUP/md/md.c b/SETUP/md/md.c
index f253bf5..8c883cc 100644
--- a/SETUP/md/md.c
+++ b/SETUP/md/md.c
@@ -78,6 +78,8 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
+#include <fcntl.h>
+#include <unistd.h>
 
 #define LINESIZE 65536  //  NeXT_MOD
 
@@ -100,10 +102,10 @@ struct dep {                    /* stores paths that a file depends on */
 } dep_files[1000];
 int dep_file_index;
 
-qsort_strcmp(a, b)
-struct dep *a, *b;
+static int
+qsort_strcmp(const void *va, const void *vb)
 {
-extern int strcmp();
+const struct dep *a = va, *b = vb;
         return strcmp(a->str, b->str);
 }
 
@@ -118,7 +120,14 @@ FILE *makout;                   /* for writing shadow */
 char makbuf[LINESIZE];          /* one line buffer for makefile */
 struct stat makstat;            /* stat of makefile for time comparisons */
 int mak_eof = 0;                        /* eof seen on makefile */
-FILE *find_mak(), *temp_mak();
+static int   read_dep(char *file);
+static void  save_dot_o(void);
+static void  parse_dep(void);
+static void  output_dep(FILE *out);
+static FILE *find_mak(char *file);
+static FILE *temp_mak(void);
+static void  expunge_mak(FILE *mak, FILE *makout);
+static void  skip_mak(FILE *mak, FILE *makout);
 
 int delete = 0;                 /* -d delete dependency file */
 int debug = 0;
@@ -138,8 +147,8 @@ char *name;
 static void scan_mak(FILE *, FILE *, char *);
 static void finish_mak(FILE *, FILE *);
 
-main(argc,argv)
-register char **argv;
+int
+main(int argc, char **argv)
 {
 int size;
 
@@ -152,7 +161,7 @@ int size;
                 if (*token++ != '-' || !*token)
                         break;
                 else { register int flag;
-                        for ( ; flag = *token++ ; ) {
+                        for ( ; (flag = (int)(unsigned char)*token++) != 0 ; ) {
                                 switch (flag) {
                                 case 'd':
                                         delete++;
@@ -177,7 +186,7 @@ int size;
                                         expunge++;
                                         break;
                                 case 'D':
-                                        for ( ; flag = *token++ ; )
+                                        for ( ; (flag = (int)(unsigned char)*token++) != 0 ; )
                                                 switch (flag) {
                                                 case 'c':
                                                         D_contents++;
@@ -211,10 +220,10 @@ newtoken: ;
         }
 
         if (!expunge && argc < 1) goto usage;
-        if ((int) outfile && (int) makefile)    /* not both */
+        if (outfile != NULL && makefile != NULL)    /* not both */
                 goto usage;
 
-        if ((int) outfile) {
+        if (outfile != NULL) {
                 /*
                  * NeXT_MOD, For SGS stuff, in case still linked to master version
                  */
@@ -227,7 +236,7 @@ newtoken: ;
                         exit(1);
                 } else if (D_open)
                         printf("%s: opened outfile \"%s\"\n", name, outfile);
-        } else if (mak = find_mak(makefile)) {
+        } else if ((mak = find_mak(makefile)) != NULL) {
                 makout = temp_mak();
                 out = makout;
                 if (expunge)
@@ -235,7 +244,7 @@ newtoken: ;
                 else
                         skip_mak(mak, makout);
         } else if (mak_eof &&  /* non existent file == mt file */
-                   (int)(makout = temp_mak())) { /* but we need to be able */
+                   (makout = temp_mak()) != NULL) { /* but we need to be able */
                 out = makout;                    /* to write here */
         } else if (makefile) {
                 fprintf(stderr, "%s: makefile \"%s\" can not be opened or stat'ed\n",
@@ -246,7 +255,7 @@ newtoken: ;
         for (; argc--; argv++) {
                 dep_file_index = 0;
 
-                if (size = read_dep(*argv)) {
+                if ((size = read_dep(*argv)) != 0) {
 
                         save_dot_o();
                         if (D_depend) printf("%s: dot_o = \"%s\"\n", name, dot_o);
@@ -269,14 +278,14 @@ usage:
 }
 
 
-read_dep(file)
-register char *file;
+static int
+read_dep(char *file)
 {
-register int fd;
-register int size;
+int fd;
+int size;
 struct stat statbuf;
 
-        if ((fd = open(file, 0)) < 0) {
+        if ((fd = open(file, O_RDONLY)) < 0) {
                 fprintf(stderr, "%s: file = \"%s\" ", name, file);
                 perror("open");
                 fflush(stdout), fflush(stderr);
@@ -294,7 +303,7 @@ struct stat statbuf;
         switch(statbuf.st_mode & S_IFMT) {
         case S_IFREG:
                 if (D_time)
-                        printf("%s: file time = %d\n", name, statbuf.st_mtime);
+                        printf("%s: file time = %ld\n", name, (long)statbuf.st_mtime);
 
                 if (statbuf.st_size > IObuffer) {
                         fprintf(stderr, "%s: file \"%s\" tooo big for IObuffer\n",
@@ -302,10 +311,10 @@ struct stat statbuf;
                         goto out;
                 } else if (force)
                         break;
-                else if ((int) mak && statbuf.st_mtime < makstat.st_mtime) {
+                else if (mak != NULL && statbuf.st_mtime < makstat.st_mtime) {
                         if (verbose || D_time)
-                                fprintf(stderr, "%s: skipping \"%s\" %d < %d \"%s\"\n",
-                                        name, file, statbuf.st_mtime, makstat.st_mtime,
+                                fprintf(stderr, "%s: skipping \"%s\" %ld < %ld \"%s\"\n",
+                                        name, file, (long)statbuf.st_mtime, (long)makstat.st_mtime,
                                         real_mak_name);
                         goto out;
                 } else /* >=   =>ok */
@@ -322,7 +331,7 @@ struct stat statbuf;
                 goto out;
         }
 
-        if ((size = read(fd, file_array, sizeof (file_array))) < 0) {
+        if ((size = (int)read(fd, file_array, sizeof(file_array))) < 0) {
                 fprintf(stderr, "%s: file = \"%s\" ", name, file);
                 perror("read");
                 fflush(stdout), fflush(stderr);
@@ -345,7 +354,8 @@ out: ;
         return 0;
 }
 
-save_dot_o()
+static void
+save_dot_o(void)
 {
 register char *cp = file_array;
 register char *svp = dot_o;
@@ -355,7 +365,8 @@ register int c;
         *svp = 0;
 }
 
-parse_dep()
+static void
+parse_dep(void)
 {
 register char *lp = file_array;
 register int c;
@@ -415,7 +426,7 @@ next_filename:
                 if (abspath)
                         *cp++ = '/';
                 for (c=0; c<i; c++) {register char *ccp = path_component[c];
-                        while (*cp++ = *ccp++);
+                        while ((*cp++ = *ccp++) != '\0');
                         *--cp = '/';
                         cp++;
                 }
@@ -425,8 +436,8 @@ next_filename:
                 dep_files[c].str = tlp;
                 dep_files[c].len = cp - tlp;
                 if (D_depend)
-                        printf("%s: dep_file[%d] = \"%s\" Len %d\n",
-                                name, dep_file_index - 1, tlp, cp - tlp);
+                        printf("%s: dep_file[%d] = \"%s\" Len %ld\n",
+                                name, dep_file_index - 1, tlp, (long)(cp - tlp));
                 tlp = cp + 1;
                 if (oldc)
                   {
@@ -436,8 +447,8 @@ next_filename:
         }
 }
 
-output_dep(out)
-FILE *out;
+static void
+output_dep(FILE *out)
 {
 register int j;
 register int size = 1000;
@@ -478,13 +489,12 @@ int written = 0;
 }
 
                 /* process makefile */
-FILE *
-find_mak(file)
-char *file;
+static FILE *
+find_mak(char *file)
 {
 FILE *mak;
 
-        if ((int) file) {
+        if (file != NULL) {
                 if ((mak = fopen(file, "r")) != NULL) {
                         real_mak_name = file;
                 } else if (update) {
@@ -514,13 +524,13 @@ FILE *mak;
         if (D_open)
                 printf("%s: opened makefile \"%s\"\n", name, real_mak_name);
         if (D_time)
-                printf("%s: makefile time = %d\n", name, makstat.st_mtime);
+                printf("%s: makefile time = %ld\n", name, (long)makstat.st_mtime);
 
         return mak;
 }
 
-FILE *
-temp_mak()
+static FILE *
+temp_mak(void)
 {
 FILE *mak;
 
@@ -543,8 +553,8 @@ FILE *mak;
         return mak;
 }
 
-skip_mak(makin, makout)
-register FILE *makin, *makout;
+static void
+skip_mak(FILE *makin, FILE *makout)
 {
 register int len = SALUTATIONLEN;
 
@@ -566,8 +576,8 @@ register int len = SALUTATIONLEN;
                 printf("eof = %d str = \"%s\"", mak_eof, makbuf);
 }
 
-expunge_mak(makin, makout)
-register FILE *makin, *makout;
+static void
+expunge_mak(FILE *makin, FILE *makout)
 {
 register int len = SALUTATIONLEN;
 register int oldlen = OLDSALUTATIONLEN;
diff --git a/bsd/conf/Makefile.template b/bsd/conf/Makefile.template
index 61a088b..91254f1 100644
--- a/bsd/conf/Makefile.template
+++ b/bsd/conf/Makefile.template
@@ -274,7 +274,6 @@ OBJS_ERROR_CAST_ALIGN =			\
 		keydb.o			\
 		keysock.o
 
-$(foreach file,$(OBJS_ERROR_CAST_ALIGN),$(eval $(call add_perfile_cflags,$(file),-Werror=cast-align)))
 
 #
 # Directories for mig generated files
diff --git a/bsd/dev/arm/unix_syscalls.c b/bsd/dev/arm/unix_syscalls.c
index ef4cf81..77b7f2a 100644
--- a/bsd/dev/arm/unix_syscalls.c
+++ b/bsd/dev/arm/unix_syscalls.c
@@ -76,6 +76,7 @@
 #include <sys/sysent.h>
 #include <sys/ucontext.h>
 #include <sys/wait.h>
+#include <sys/kauth.h>
 #include <mach/thread_act.h>    /* for thread_abort_safely */
 #include <mach/thread_status.h>
 
@@ -85,11 +86,13 @@
 #include <sys/sdt.h>
 
 #include <security/audit/audit.h>
+#include <security/mac_mach_internal.h>
 
 #include <mach/branch_predicates.h>
 
 /* dynamically generated at build time based on syscalls.master */
 extern const char *syscallnames[];
+extern void *find_user_regs(thread_t);
 
 /*
  * Function:	unix_syscall
@@ -196,7 +199,7 @@ void unix_syscall(arm_saved_state_t * state)
 #endif
 
 #if CONFIG_MACF
-    mac_thread_userret(code, error, thread);
+    mac_thread_userret(thread);
 #endif
 
     if (error)
diff --git a/bsd/kern/kdebug.c b/bsd/kern/kdebug.c
index dfc3873..75cc604 100644
--- a/bsd/kern/kdebug.c
+++ b/bsd/kern/kdebug.c
@@ -75,6 +75,8 @@
 /* XXX should have prototypes, but Mach does not provide one */
 void task_act_iterate_wth_args(task_t, void(*)(thread_t, void *), void *);
 int cpu_number(void);	/* XXX <machine/...> include path broken */
+void disable_preemption(void);
+void enable_preemption(void);
 
 /* XXX should probably be static, but it's debugging code... */
 int kdbg_read(user_addr_t, size_t *, vnode_t, vfs_context_t);
diff --git a/bsd/kern/kern_exec.c b/bsd/kern/kern_exec.c
index eae4a73..b989b6e 100644
--- a/bsd/kern/kern_exec.c
+++ b/bsd/kern/kern_exec.c
@@ -167,6 +167,7 @@ void vfork_exit(proc_t p, int rv);
 int setsigvec(proc_t, thread_t, int, struct __kern_sigaction *, boolean_t in_sigstart);
 extern void proc_apply_task_networkbg_internal(proc_t, thread_t);
 int task_set_cpuusage(task_t task, uint64_t percentage, uint64_t interval, uint64_t deadline, int scope);
+extern void pal_dbg_set_task_name(task_t task);
 
 /*
  * Mach things for which prototypes are unavailable from Mach headers
diff --git a/bsd/kern/mach_loader.c b/bsd/kern/mach_loader.c
index 3ae9d85..23c7b60 100644
--- a/bsd/kern/mach_loader.c
+++ b/bsd/kern/mach_loader.c
@@ -92,6 +92,7 @@ extern int	get_map_nentries(vm_map_t);
 
 extern kern_return_t	memory_object_signed(memory_object_control_t control,
 					     boolean_t is_signed);
+extern void		kprintf(const char *fmt, ...);
 
 /* An empty load_result_t */
 static load_result_t load_result_null = {
diff --git a/libkern/kxld/kxld_object.h b/libkern/kxld/kxld_object.h
index ab78f20..0836934 100644
--- a/libkern/kxld/kxld_object.h
+++ b/libkern/kxld/kxld_object.h
@@ -56,7 +56,7 @@ kern_return_t kxld_object_init_from_macho(KXLDObject *object,
     u_char *file, u_long size, const char *name,
     struct kxld_array *section_order,
     cpu_type_t cputype, cpu_subtype_t cpusubtype, KXLDFlags flags)
-    __attribute__((nonnull(1,2,4) visibility("hidden")));
+    __attribute__((nonnull(1,2,4), visibility("hidden")));
 
 void kxld_object_clear(KXLDObject *object)
     __attribute__((nonnull, visibility("hidden")));
@@ -158,4 +158,3 @@ kern_return_t kxld_object_process_relocations(KXLDObject *object,
     __attribute__((nonnull, visibility("hidden")));
 
 #endif /* _KXLD_OBJECT_H_ */
-
diff --git a/libkern/mkext.c b/libkern/mkext.c
index 86238fc..1b47b30 100644
--- a/libkern/mkext.c
+++ b/libkern/mkext.c
@@ -47,6 +47,7 @@ mkext_adler32(uint8_t *buf, int32_t len)
     int k;
 
 #if defined _ARM_ARCH_6
+    extern unsigned long adler32_vec(unsigned long adler, unsigned long sum2, const uint8_t *buf, unsigned int len);
 
 	/* align buf to 16-byte boundary */
     while ((((uintptr_t)buf)&15)&&(len>0)) { /* not on a 16-byte boundary */
@@ -284,4 +285,3 @@ static void delete_node(struct encode_state *sp, int p)
 }
 
 #endif /* !KERNEL */
-
diff --git a/makedefs/MakeInc.def b/makedefs/MakeInc.def
index ee67e78..c92f805 100644
--- a/makedefs/MakeInc.def
+++ b/makedefs/MakeInc.def
@@ -438,7 +438,7 @@ S_KCC	= $(CC)
 #
 # Default SFLAGS
 #
-export SFLAGS_GEN = -D__ASSEMBLER__ $(OTHER_CFLAGS)
+export SFLAGS_GEN = -D__ASSEMBLER__ -x assembler-with-cpp $(OTHER_CFLAGS)
 
 export SFLAGS_RELEASE 	= 
 export SFLAGS_DEVELOPMENT 	= 
diff --git a/osfmk/arm/bcopyinout.s b/osfmk/arm/bcopyinout.s
index ab024ef..f22aad4 100644
--- a/osfmk/arm/bcopyinout.s
+++ b/osfmk/arm/bcopyinout.s
@@ -64,16 +64,16 @@ EnterARM(copyinmsg)
     mov     r3, #0
     LoadThreadRegister(r10)
     adr     r12, .Lcopyin_fault
-    str     r12, [r10, TH_RECOVER]
+    str     r12, [r10, #TH_RECOVER]
     bl      .Lcopyin_guts
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
     mov     r0, #0
     ldmfd   sp!,{r10,r11,pc}
 
 .Lcopyin_fault:
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
 	mov	r0, #0xE    /* EFAULT */
 	cmp	r3, #0x00
 	ldmgtfd	sp!, {r4-r7}		/* r3 > 0 Restore r4-r7 */
@@ -483,17 +483,17 @@ EnterARM(copyoutmsg)
     LoadThreadRegister(r10)
     mov     r3, #0
     adr     r12, .Lcopyout_fault
-    ldr     r11, [r10, TH_RECOVER]
-    str     r12, [r10, TH_RECOVER]
+    ldr     r11, [r10, #TH_RECOVER]
+    str     r12, [r10, #TH_RECOVER]
     bl  .Lcopyout_guts
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
     mov r0, #0
     ldmfd   sp!,{r10,r11,pc}
 
 .Lcopyout_fault:
     mov     r11, #0
-    str     r11, [r10, TH_RECOVER]
+    str     r11, [r10, #TH_RECOVER]
 	mov r0, #0x0e
 	cmp	r3, #0x00
 	ldmgtfd	sp!, {r4-r7}		/* r3 > 0 Restore r4-r7 */
@@ -947,7 +947,7 @@ EnterARM(copyinstr)
    LoadThreadRegister(r4)
 
 	adr	r5, .Lcopystrfault
-	str	r5, [r4, TH_RECOVER]
+	str	r5, [r4, #TH_RECOVER]
 
 1:	ldrbt	r5, [r0], #0x0001
 	add	r6, r6, #0x00000001
@@ -957,7 +957,7 @@ EnterARM(copyinstr)
 	bne	1b
 
 	mov	r0, #0x00000000
-	str	r0, [r4, TH_RECOVER]
+	str	r0, [r4, #TH_RECOVER]
 
 	teq	r5, #0x00000000
 	moveq	r0, #0x00000000
@@ -988,7 +988,7 @@ EnterARM(copyoutstr)
     LoadThreadRegister(r4)
 
 	adr	r5, .Lcopystrfault
-	str	r5, [r4, TH_RECOVER]
+	str	r5, [r4, #TH_RECOVER]
 
 1:	ldrb	r5, [r0], #0x0001
 	add	r6, r6, #0x00000001
@@ -998,7 +998,7 @@ EnterARM(copyoutstr)
 	bne	1b
 
 	mov	r0, #0x00000000
-	str	r0, [r4, TH_RECOVER]
+	str	r0, [r4, #TH_RECOVER]
 
 	teq	r5, #0x00000000
 	moveq	r0, #0x00000000
@@ -1013,7 +1013,7 @@ EnterARM(copyoutstr)
 /* A fault occurred during the copy */
 .Lcopystrfault:
 	mov	r1, #0x00000000
-	str	r1, [r4, TH_RECOVER]
+	str	r1, [r4, #TH_RECOVER]
 	RESTORE_REGS
     bx      lr
 
@@ -1058,8 +1058,8 @@ EnterARM(copyinframe)
 	stmfd	sp!,{r4}
 	adr 	r3, _copyinframe_error
 	LoadThreadRegister(r12)
-	ldr 	r4, [r12, TH_RECOVER]
-	str 	r3, [r12, TH_RECOVER]
+	ldr 	r4, [r12, #TH_RECOVER]
+	str 	r3, [r12, #TH_RECOVER]
 	ldmia	r0, {r2,r3}
 	stmia 	r1, {r2,r3}
 _copyinframe_success:
diff --git a/osfmk/arm/cswitch.s b/osfmk/arm/cswitch.s
index 444ce5b..7645e57 100644
--- a/osfmk/arm/cswitch.s
+++ b/osfmk/arm/cswitch.s
@@ -43,7 +43,7 @@
 EnterARM(Call_continuation)
     /* Set the new stack pointer. */
     LoadThreadRegister(r12)
-    ldr     sp, [r12, TH_PCB_ISS]
+    ldr     sp, [r12, #TH_PCB_ISS]
 
     /* Zero out frame pointer */
     mov     r7, #0
@@ -70,10 +70,10 @@ EnterARM(Call_continuation)
 EnterARM(Switch_context)
     /* Switch context to the new thread and save the continuation. */
     teq     r1, #0
-    strne   r1, [r0, TH_CONTINUATION]
+    strne   r1, [r0, #TH_CONTINUATION]
 
     /* Only store registers if there is a continuation */
-    ldreq   r3, [r0, TH_PCB_ISS]
+    ldreq   r3, [r0, #TH_PCB_ISS]
     addeq   r3, r3, #16
     stmiaeq r3!, {r4-lr}
 
@@ -84,11 +84,11 @@ EnterARM(Switch_context)
     /* Set old/new threads */
     mcr     p15, 0, r2, c13, c0, 4
 
-    ldr     r3, [r2, MACHINE_THREAD_CTHREAD_SELF]
+    ldr     r3, [r2, #MACHINE_THREAD_CTHREAD_SELF]
     mcr     p15, 0, r3, c13, c0, 3
 
     /* Load registers and go. */
-    ldr     r3, [r2, TH_PCB_ISS]
+    ldr     r3, [r2, #TH_PCB_ISS]
     add     r3, r3, #16
     ldmia   r3!, {r4-lr}
 
@@ -115,11 +115,11 @@ EnterARM(machine_load_context)
     mcr     p15, 0, r0, c13, c0, 4
 
     /* Set cthread value */
-    ldr     r1, [r0, MACHINE_THREAD_CTHREAD_SELF]
+    ldr     r1, [r0, #MACHINE_THREAD_CTHREAD_SELF]
     mrc     p15, 0, r2, c13, c0, 3
 
     /* Load registers and go. */
-    ldr     r3, [r0, TH_PCB_ISS]
+    ldr     r3, [r0, #TH_PCB_ISS]
     mov     r0, #0
 
     add     r3, r3, #16
@@ -132,7 +132,7 @@ EnterARM(machine_load_context)
 EnterARM(thread_syscall_return)
     cpsid   i
     LoadThreadRegister(r12)
-    ldr     r4, [r12, TH_PCB_USS]
+    ldr     r4, [r12, #TH_PCB_USS]
     str     r0, [r4]
     b       thread_return_join
 
@@ -150,8 +150,8 @@ thread_return_join:
     /* Check for pending ast */
     LoadThreadRegister(r12)
 
-    ldr     r8, [r12, MACHINE_THREAD_CPU_DATA]
-    ldr     r5, [r8, CPU_PENDING_AST]
+    ldr     r8, [r12, #MACHINE_THREAD_CPU_DATA]
+    ldr     r5, [r8, #CPU_PENDING_AST]
 
     cmp     r5, #0
     beq     return_to_user
@@ -167,7 +167,7 @@ thread_return_join:
 return_to_user:
     /* Restore registers */
     LoadThreadRegister(r12)
-    ldr     sp, [r12, TH_PCB_USS]
+    ldr     sp, [r12, #TH_PCB_USS]
     ldr     r0, [sp, #0x40]
 
     msr     spsr_cxsf, r0
diff --git a/osfmk/arm/locore.s b/osfmk/arm/locore.s
index 9f6a2ea..0c36da4 100644
--- a/osfmk/arm/locore.s
+++ b/osfmk/arm/locore.s
@@ -85,21 +85,21 @@ mmu_reinitialize:
      * Create a dumb mapping for right now. This mapping lies
      * at the top of kernel data.
      */
-    ldr     r4, [r0, BOOT_ARGS_TOP_OF_KERNEL]
-    ldr     r10, [r0, BOOT_ARGS_VIRTBASE]
-    ldr     r11, [r0, BOOT_ARGS_PHYSBASE]
+    ldr     r4, [r0, #BOOT_ARGS_TOP_OF_KERNEL]
+    ldr     r10, [r0, #BOOT_ARGS_VIRTBASE]
+    ldr     r11, [r0, #BOOT_ARGS_PHYSBASE]
 
     /* Is it bootArgs revision 3? */
-    ldrh    r12, [r0, BOOT_ARGS_VERSION]
+    ldrh    r12, [r0, #BOOT_ARGS_VERSION]
     cmp     r12, #3
 
     /* Align the memory size to 1MB for compatibility. */
-    ldreq   r5, [r0, BOOT_ARGS_MEMSIZE]
+    ldreq   r5, [r0, #BOOT_ARGS_MEMSIZE]
     andeq   r5, r5, #0xFFF000000
-    streq   r5, [r0, BOOT_ARGS_MEMSIZE]
+    streq   r5, [r0, #BOOT_ARGS_MEMSIZE]
 
     /* Load memory size value after fixup. */
-    ldr     r12, [r0, BOOT_ARGS_MEMSIZE]
+    ldr     r12, [r0, #BOOT_ARGS_MEMSIZE]
 
     /* MMU cacheability value. */
     orr     r5, r4, #0x18
@@ -242,9 +242,9 @@ mmu_initialized:
  */
 EnterARM(sleep_test)
     /* Get physical base. */
-    ldr     r8, [r1, BOOT_ARGS_PHYSBASE]
-    ldr     r9, [r1, BOOT_ARGS_VIRTBASE]
-    ldr     r4, [r1, BOOT_ARGS_TOP_OF_KERNEL]
+    ldr     r8, [r1, #BOOT_ARGS_PHYSBASE]
+    ldr     r9, [r1, #BOOT_ARGS_VIRTBASE]
+    ldr     r4, [r1, #BOOT_ARGS_TOP_OF_KERNEL]
 
     /* Set new page tables. (kernel bootstrap page table) */
     orr     r6, r4, #0x18
@@ -255,7 +255,7 @@ EnterARM(sleep_test)
     add     r4, r4, r9
 
     /* Create boot page table entry for trampoline. */
-    ldr     r10, [r1, BOOT_ARGS_MEMSIZE]
+    ldr     r10, [r1, #BOOT_ARGS_MEMSIZE]
     mov     r6, #0xE
     mov     r1, #1
     orr     r6, r6, r1, lsl#10
diff --git a/osfmk/arm/loose_ends.c b/osfmk/arm/loose_ends.c
index 9e34d9e..6132a78 100644
--- a/osfmk/arm/loose_ends.c
+++ b/osfmk/arm/loose_ends.c
@@ -436,11 +436,11 @@ kern_return_t copypv(addr64_t src64, addr64_t snk64, unsigned int size, int whic
      * If it's a kernel copy, use ovbcopy. 
      */
     if (!(which & (cppvKmap | cppvPsrc)))
-        copyout(src64, snk64, size);
-    else if (!(which & (cppvKmap | cppvPsrc)))
-        copyin(src64, snk64, size);
+        copyout((const void *)(uintptr_t)src64, (user_addr_t)snk64, size);
+    else if (!(which & (cppvKmap | cppvPsnk)))
+        copyin((user_addr_t)src64, (void *)(uintptr_t)snk64, size);
     else
-        bcopy(src64, snk64, size);
+        bcopy((const void *)(uintptr_t)src64, (void *)(uintptr_t)snk64, size);
 
     return KERN_SUCCESS;
 }
diff --git a/osfmk/arm/machine_routines.h b/osfmk/arm/machine_routines.h
index 389671f..176e9bf 100644
--- a/osfmk/arm/machine_routines.h
+++ b/osfmk/arm/machine_routines.h
@@ -44,6 +44,9 @@ __BEGIN_DECLS
 // why
 void bcopy_phys(addr64_t src64, addr64_t dst64, vm_size_t bytes);
 
+/* Defined in osfmk/arm/machine_routines_asm.s */
+void Halt_system(void);
+
 #ifdef XNU_KERNEL_PRIVATE
 
 /* are we a 64 bit platform ? */
diff --git a/osfmk/arm/machine_routines_asm.s b/osfmk/arm/machine_routines_asm.s
index 9604835..4b93f97 100644
--- a/osfmk/arm/machine_routines_asm.s
+++ b/osfmk/arm/machine_routines_asm.s
@@ -116,8 +116,8 @@ EnterARM(ml_set_interrupts_enabled)
 
 astloop:
     LoadThreadRegister(r12)
-    ldr     r1, [r12, MACHINE_THREAD_CPU_DATA]
-    ldr     r0, [r1, CPU_PENDING_AST]
+    ldr     r1, [r12, #MACHINE_THREAD_CPU_DATA]
+    ldr     r0, [r1, #CPU_PENDING_AST]
 
     stmfd   sp!,{r0,r2,r7,lr}
     bl      _get_preemption_level
@@ -181,7 +181,7 @@ EnterARM(disable_preemption)
  */
 EnterARM(get_preemption_level)
     LoadThreadRegister(r12)
-    ldr     r0, [r12, MACHINE_THREAD_PREEMPT_COUNT]
+    ldr     r0, [r12, #MACHINE_THREAD_PREEMPT_COUNT]
     bx      lr
 
 /**
@@ -194,9 +194,9 @@ EnterARM(_enable_preemption)
 EnterARM(enable_preemption)
     /* Get thread ID */
     LoadThreadRegister(r12)
-    ldr     r2, [r12, MACHINE_THREAD_PREEMPT_COUNT]
+    ldr     r2, [r12, #MACHINE_THREAD_PREEMPT_COUNT]
     subs    r2, r2, #1
-    strne   r2, [r12, MACHINE_THREAD_PREEMPT_COUNT]
+    strne   r2, [r12, #MACHINE_THREAD_PREEMPT_COUNT]
     bxne    lr
 
     /* Preempt. */
@@ -205,13 +205,13 @@ EnterARM(enable_preemption)
     /* Check for interrupts */
     mrs     r3, cpsr
     tst     r3, #0x80
-    strne   r2, [r12, MACHINE_THREAD_PREEMPT_COUNT]
+    strne   r2, [r12, #MACHINE_THREAD_PREEMPT_COUNT]
     bxne    lr
 
     /* Get CPU data and add an AST. */
-    ldr     r1, [r12, MACHINE_THREAD_CPU_DATA]
-    ldr     r0, [r1, CPU_PENDING_AST]
-    str     r2, [r12, MACHINE_THREAD_PREEMPT_COUNT]
+    ldr     r1, [r12, #MACHINE_THREAD_CPU_DATA]
+    ldr     r0, [r1, #CPU_PENDING_AST]
+    str     r2, [r12, #MACHINE_THREAD_PREEMPT_COUNT]
 
     ands    r1, r0, #4
     bne     __preempt
diff --git a/osfmk/arm/misc_protos.h b/osfmk/arm/misc_protos.h
index 4cd4969..4d3d946 100644
--- a/osfmk/arm/misc_protos.h
+++ b/osfmk/arm/misc_protos.h
@@ -75,7 +75,12 @@ void sleh_abort(void *context, int reason);
 
 void cache_initialize(void);
 
-void get_cachetype_cp15();
+void get_cachetype_cp15(void);
 void identify_arm_cpu(void);
+void machine_startup(void);
+void set_mmu_ttb(uint32_t ttb);
+void set_mmu_ttb_alt(uint32_t ttb_alt);
+void set_mmu_ttbcr(uint32_t ttbcr);
+void flush_mmu_tlb(void);
 
 #endif
diff --git a/osfmk/arm/pal.c b/osfmk/arm/pal.c
index ad03c45..fec2ab6 100644
--- a/osfmk/arm/pal.c
+++ b/osfmk/arm/pal.c
@@ -33,6 +33,7 @@
 
 #include <mach/mach_types.h>
 #include <machine/pal_routines.h>
+#include <pexpert/arm/protos.h>
 
 /* Serial routines */
 int pal_serial_init(void)
diff --git a/osfmk/arm/pcb.c b/osfmk/arm/pcb.c
index d0596fe..c60be5b 100644
--- a/osfmk/arm/pcb.c
+++ b/osfmk/arm/pcb.c
@@ -419,13 +419,13 @@ kern_return_t machine_thread_dup(thread_t self, thread_t target)
     /*
      * Copy user registers.
      */
-    ovbcopy(self->machine.uss, target->machine.uss, sizeof(arm_saved_state_t));
+    bcopy(self->machine.uss, target->machine.uss, sizeof(arm_saved_state_t));
 
     /*
      * Copy vfp registers.
      */
     save_vfp_context(self);
-    ovbcopy(self->machine.uvfp, target->machine.uvfp, sizeof(arm_vfp_state_t));
+    bcopy(self->machine.uvfp, target->machine.uvfp, sizeof(arm_vfp_state_t));
 
     /*
      * Duplicate self.
diff --git a/osfmk/arm/pmap.h b/osfmk/arm/pmap.h
index 9544c49..2d66846 100644
--- a/osfmk/arm/pmap.h
+++ b/osfmk/arm/pmap.h
@@ -266,6 +266,8 @@ extern int pmap_map_block_rc(pmap_t pmap, addr64_t va, ppnum_t pa,
                              unsigned int flags);
 
 extern ppnum_t pmap_find_phys(pmap_t pmap, addr64_t va);
+extern ppnum_t pmap_find_phys_fvtp(pmap_t pmap, addr64_t va);
+extern void pmap_expand(pmap_t map, vm_offset_t v);
 extern void MapUserMemoryWindowInit(void);
 extern addr64_t MapUserMemoryWindow(vm_map_t map, addr64_t va);
 extern boolean_t pmap_eligible_for_execute(ppnum_t pa);
diff --git a/osfmk/arm/trap.c b/osfmk/arm/trap.c
index 1c5533e..8d54493 100644
--- a/osfmk/arm/trap.c
+++ b/osfmk/arm/trap.c
@@ -51,6 +51,9 @@
 #include <vm/vm_kern.h>         /* For kernel_map */
 #include <libkern/OSByteOrder.h>
 #include <arm/armops.h>
+#include <kdp/kdp.h>
+extern char *proc_name_address(void *p);
+extern int proc_pid(void *p);
 
 #define ANSI_COLOR_RED     "\x1b[31m"
 #define ANSI_COLOR_GREEN   "\x1b[32m"
@@ -250,8 +253,8 @@ void sleh_abort(void *context, int reason)
                 if (code != KERN_SUCCESS) {
 
                     if (current_debugger) {
-                        if (kdp_raise_exception(EXC_BREAKPOINT, 0, 0, arm_ctx))
-                            return;
+                        kdp_raise_exception(EXC_BREAKPOINT, 0, 0, arm_ctx);
+                        return;
                     }
 
                     /*
@@ -337,7 +340,7 @@ void sleh_abort(void *context, int reason)
 
                             arm_ctx->pc = thread->recover;
                             arm_ctx->cpsr &= ~(1 << 5);
-                            thread->recover = NULL;
+                            thread->recover = 0;
                             return;
                         }
                     }
@@ -524,7 +527,7 @@ boolean_t irq_handler(void *context)
     /*
      * Disable system preemption, dispatch the interrupt and go.
      */
-    __disable_preemption();
+    mp_disable_preemption();
 
     /*
      * Dispatch the interrupt.
@@ -536,7 +539,7 @@ boolean_t irq_handler(void *context)
      */
     datap->cpu_interrupt_level--;
 
-    __enable_preemption();
+    mp_enable_preemption();
 
     return ret;
 }
@@ -601,7 +604,7 @@ void sleh_undef(arm_saved_state_t * state)
          */
         spl_t spl = splhigh();
         thumb_offset = (arm_ctx->cpsr & (1 << 5)) ? 1 : 0;
-        copyin((uint8_t *) (arm_ctx->pc + thumb_offset), &instruction,
+        copyin((user_addr_t)(arm_ctx->pc + thumb_offset), &instruction,
                sizeof(uint32_t));
         splx(spl);
 
diff --git a/osfmk/arm/traps_lo.s b/osfmk/arm/traps_lo.s
index d3f2d54..e6e4b2f 100644
--- a/osfmk/arm/traps_lo.s
+++ b/osfmk/arm/traps_lo.s
@@ -65,7 +65,7 @@ undef_from_user:
 
     /* Usermode undefined, maybe it's a VFP instruction, HUH?! */
     LoadThreadRegister(sp)
-    ldr     sp, [sp, TH_PCB_USS]
+    ldr     sp, [sp, #TH_PCB_USS]
     stmea   sp, {r0-lr}^
 
     str     lr, [sp, #0x3C]
@@ -76,7 +76,7 @@ undef_from_user:
     /* Change to supervisor */
     cpsid   i, #0x13
     LoadThreadRegister(r1)
-    ldr     sp, [r1, TH_PCB_ISS]
+    ldr     sp, [r1, #TH_PCB_ISS]
     blx     _sleh_undef
     b       _thread_exception_return
 
@@ -118,7 +118,7 @@ EnterARM(fleh_swi)
 
     /* Save current registers and spsr */
     LoadThreadRegister(sp)
-    ldr     sp, [sp, TH_PCB_USS]
+    ldr     sp, [sp, #TH_PCB_USS]
     stmea   sp, {r0-lr}^
 
     str     lr, [sp, #0x3C]
@@ -128,7 +128,7 @@ EnterARM(fleh_swi)
     mov     r8, sp
 
     LoadThreadRegister(sp)
-    ldr     sp, [sp, TH_PCB_ISS]
+    ldr     sp, [sp, #TH_PCB_ISS]
 
     mov     r11, r12
     cpsie   i
@@ -221,7 +221,7 @@ thread_get_cthread_trap:
     /* Get the current cthread value and return to user. */
     blx     _thread_get_cthread_self
     LoadThreadRegister(r1)
-    ldr     r1, [r1, TH_PCB_USS]
+    ldr     r1, [r1, #TH_PCB_USS]
     str     r0, [r1]
     bl      _thread_exception_return
     mov     r0, #0x83
@@ -246,7 +246,7 @@ EnterARM(fleh_prefabt)
 prefetch_abort_in_user:
     /* Oh well, not now. */
     LoadThreadRegister(sp)
-    ldr     sp, [sp, TH_PCB_USS]
+    ldr     sp, [sp, #TH_PCB_USS]
     stmea   sp, {r0-lr}^
 
     /* Save lr */
@@ -270,7 +270,7 @@ prefetch_abort_in_user:
     mov     r0, sp
 
     LoadThreadRegister(r1)
-    ldr     sp, [r1, TH_PCB_ISS]
+    ldr     sp, [r1, #TH_PCB_ISS]
 
     mov     r1, #3      /* Prefetch Abort */
     blx     _sleh_abort
@@ -333,7 +333,7 @@ EnterARM(fleh_dataabt)
 data_abort_crash_in_usermode:
     /* Oh well, not now. */
     LoadThreadRegister(sp)
-    ldr     sp, [sp, TH_PCB_USS]
+    ldr     sp, [sp, #TH_PCB_USS]
     stmea   sp, {r0-lr}^
 
     /* Save lr */
@@ -357,7 +357,7 @@ data_abort_crash_in_usermode:
     cpsid   i, #0x13
 
     LoadThreadRegister(r1)
-    ldr     sp, [r1, TH_PCB_ISS]
+    ldr     sp, [r1, #TH_PCB_ISS]
 
     mov     r1, #4      /* Data Abort */
     blx     _sleh_abort
@@ -428,7 +428,7 @@ EnterARM(fleh_irq)
 
 irqhandler_from_user:
     LoadThreadRegister(sp)
-    ldr     sp, [sp, TH_PCB_USS]
+    ldr     sp, [sp, #TH_PCB_USS]
     stmea   sp, {r0-lr}^
     str     lr, [sp, #0x3C]
     mrs     r0, spsr
@@ -484,7 +484,7 @@ irq_restore_kernel:
 
     /* Threads. */
     LoadThreadRegister(r12)
-    ldr     r0, [r12, MACHINE_THREAD_PREEMPT_COUNT]
+    ldr     r0, [r12, #MACHINE_THREAD_PREEMPT_COUNT]
 
     /* Preempt thread if necessary */
     bne     restore_kernel_context
@@ -527,7 +527,7 @@ irq_preempt:
 irq_restore_user:
     cpsid   i, #0x13
     LoadThreadRegister(sp)
-    ldr     sp, [sp, TH_PCB_ISS]
+    ldr     sp, [sp, #TH_PCB_ISS]
     b       _thread_exception_return
 
 return_to_kernel:
@@ -543,7 +543,7 @@ return_to_kernel:
 
     /* Check preemption count */
     LoadThreadRegister(r12)
-    ldr     r0, [r12, MACHINE_THREAD_PREEMPT_COUNT]
+    ldr     r0, [r12, #MACHINE_THREAD_PREEMPT_COUNT]
     cmp     r0, #0
     bne     restart_kernel_execution
 
diff --git a/osfmk/conf/Makefile.arm b/osfmk/conf/Makefile.arm
index a6d9b9f..879b8d6 100644
--- a/osfmk/conf/Makefile.arm
+++ b/osfmk/conf/Makefile.arm
@@ -8,14 +8,14 @@ export CFLAGS_DEVELOPMENTARM = -O0
 export CFLAGS_DEBUGARM = -O0
 export CFLAGS_PROFILEARM = -O0
 
-bcopyinout.o_SFLAGS_ADD += -no-integrated-as
+bcopyinout.o_SFLAGS_ADD += -fno-integrated-as
 
 ifeq (-arch armv6,$(ARCH_FLAGS_ARM))
-locore.o_SFLAGS_ADD  += -no-integrated-as
-machine_routines_asm.o_SFLAGS_ADD += -no-integrated-as
-hw_lock.o_SFLAGS_ADD  += -no-integrated-as
-cache.o_SFLAGS_ADD += -no-integrated-as
-tlb-arm11.o_SFLAGS_ADD += -no-integrated-as
+locore.o_SFLAGS_ADD  += -fno-integrated-as
+machine_routines_asm.o_SFLAGS_ADD += -fno-integrated-as
+hw_lock.o_SFLAGS_ADD  += -fno-integrated-as
+cache.o_SFLAGS_ADD += -fno-integrated-as
+tlb-arm11.o_SFLAGS_ADD += -fno-integrated-as
 endif
 
 ifeq (-arch armv7,$(ARCH_FLAGS_ARM))
diff --git a/osfmk/conf/Makefile.arm64 b/osfmk/conf/Makefile.arm64
index 71cf1bb..a368821 100644
--- a/osfmk/conf/Makefile.arm64
+++ b/osfmk/conf/Makefile.arm64
@@ -1,2 +1,2 @@
 # pain.
-bcopyinout.o_SFLAGS_ADD += -no-integrated-as
\ No newline at end of file
+bcopyinout.o_SFLAGS_ADD += -fno-integrated-as
diff --git a/osfmk/conf/files.arm b/osfmk/conf/files.arm
index 7a82f36..9c1e375 100644
--- a/osfmk/conf/files.arm
+++ b/osfmk/conf/files.arm
@@ -34,7 +34,7 @@ osfmk/arm/loose_ends.c	standard
 osfmk/arm/commpage/commpage.c	standard
 osfmk/arm/atomic.s		standard
 osfmk/arm/bcopy.s		standard
-osfmk/arm/bcopyinout.s		standard
+osfmk/arm/bcopyinout_compat.c	standard
 osfmk/arm/bzero.s		standard
 osfmk/arm/cpu_number.s		standard
 osfmk/arm/cswitch.s		standard
@@ -67,4 +67,3 @@ osfmk/arm/crt/umodsi3.s		standard
 osfmk/kdp/ml/arm/kdp_arm_common.c      optional        mach_kdp
 osfmk/kdp/ml/arm/kdp_machdep.c         optional        mach_kdp
 osfmk/kdp/ml/arm/kdp_vm.c              optional        mach_kdp
-
diff --git a/osfmk/console/arm/serial_console.c b/osfmk/console/arm/serial_console.c
index 993877c..7e32f7d 100644
--- a/osfmk/console/arm/serial_console.c
+++ b/osfmk/console/arm/serial_console.c
@@ -35,6 +35,8 @@
 #include <console/video_console.h>
 #include <console/serial_protos.h>
 #include <kern/kalloc.h>
+#include <kern/simple_lock.h>
+#include <pexpert/arm/protos.h>
 
 static struct {
 	char	*buffer;
@@ -232,7 +234,7 @@ console_ring_try_empty(void)
 	/* Indicate that we're in the process of writing a block of data
 	 * to the console.
 	 */
-	atomic_incl(&console_output, 1);
+	(void)hw_atomic_add((volatile uint32_t *)&console_output, 1);
 	for (;;) {
 		char	ch;
 		if (!state)
@@ -244,7 +246,7 @@ console_ring_try_empty(void)
 			break;
 		_cnputc(ch);
 	}
-	atomic_decl(&console_output, 1);
+	(void)hw_atomic_sub((volatile uint32_t *)&console_output, 1);
 	simple_unlock(&console_ring.read_lock);
 }
 
diff --git a/osfmk/kdp/ml/arm/kdp_machdep.c b/osfmk/kdp/ml/arm/kdp_machdep.c
index b2a7ebe..a3afd25 100644
--- a/osfmk/kdp/ml/arm/kdp_machdep.c
+++ b/osfmk/kdp/ml/arm/kdp_machdep.c
@@ -440,11 +440,10 @@ void kdp_machine_init(void) {
 
     vm_map_unlock(kernel_map);
 
-    debugger_ptep = pmap_pte(kernel_pmap, debugger_window_kva);
+    debugger_ptep = (pt_entry_t *)pmap_pte(kernel_pmap, debugger_window_kva);
 
     if (debugger_ptep == NULL) {
         pmap_expand(kernel_pmap, debugger_window_kva);
-        debugger_ptep = pmap_pte(kernel_pmap, debugger_window_kva);
+        debugger_ptep = (pt_entry_t *)pmap_pte(kernel_pmap, debugger_window_kva);
     }
 }
-
diff --git a/osfmk/kern/kmod.c b/osfmk/kern/kmod.c
index 160c4bd..69d81d2 100644
--- a/osfmk/kern/kmod.c
+++ b/osfmk/kern/kmod.c
@@ -85,7 +85,7 @@ extern void proc_selfname(char * buf, int size);
             __FUNCTION__, procname);  \
     } while (0)
 
-#if __i386__
+#if __i386__ || __arm__
 // in libkern/OSKextLib.cpp
 extern kern_return_t kext_get_kmod_info(
     kmod_info_array_t      * kmod_list,
@@ -93,7 +93,7 @@ extern kern_return_t kext_get_kmod_info(
 #define KMOD_MIG_UNUSED
 #else
 #define KMOD_MIG_UNUSED __unused
-#endif /* __i386__ */
+#endif /* __i386__ || __arm__ */
 
 
 /*********************************************************************
diff --git a/osfmk/libsa/i386/types.h b/osfmk/libsa/i386/types.h
index 3cbf438..5a03c7d 100644
--- a/osfmk/libsa/i386/types.h
+++ b/osfmk/libsa/i386/types.h
@@ -56,7 +56,10 @@
 #ifndef	_MACH_MACHINE_TYPES_H_
 #define _MACH_MACHINE_TYPES_H_ 1
 
-typedef int		dev_t;		/* device number (major+minor) */
+#ifndef _DEV_T
+#define _DEV_T
+typedef unsigned short  dev_t;          /* device number (major+minor) */
+#endif
 
 typedef signed char	bit8_t;		/* signed 8-bit quantity */
 typedef unsigned char	u_bit8_t;	/* unsigned 8-bit quantity */
diff --git a/osfmk/mach/mig.h b/osfmk/mach/mig.h
index 3d76550..beb67b1 100644
--- a/osfmk/mach/mig.h
+++ b/osfmk/mach/mig.h
@@ -53,6 +53,15 @@
 #define __MigKernelSpecificCode 1
 #define _MIG_KERNEL_SPECIFIC_CODE_ 1
 
+/* Compatibility shim for older trees built with newer MIG outputs. */
+#ifndef MACH_MSG_DESTROY_FROM_KERNEL_COMPAT
+#define MACH_MSG_DESTROY_FROM_KERNEL_COMPAT 1
+static __inline__ void mach_msg_destroy_from_kernel(mach_msg_header_t *msg)
+{
+	(void)msg;
+}
+#endif
+
 #elif !defined(__MigTypeCheck)
 
 #if defined(TypeCheck)
diff --git a/osfmk/vm/vm_pageout.c b/osfmk/vm/vm_pageout.c
index 2bfd6e7..8eec348 100644
--- a/osfmk/vm/vm_pageout.c
+++ b/osfmk/vm/vm_pageout.c
@@ -3022,7 +3022,7 @@ uint32_t vm_pageout_considered_page_last = 0;
  * called once per-second via "compute_averages"
  */
 void
-compute_pageout_gc_throttle()
+compute_pageout_gc_throttle(void *arg __unused)
 {
 	if (vm_pageout_considered_page != vm_pageout_considered_page_last) {
 
diff --git a/pexpert/arm/common/pe_init.c b/pexpert/arm/common/pe_init.c
index 56f03c5..36c8b2e 100644
--- a/pexpert/arm/common/pe_init.c
+++ b/pexpert/arm/common/pe_init.c
@@ -31,6 +31,7 @@
  */
 
 #include <sys/types.h>
+#include <sys/systm.h>
 #include <mach/vm_param.h>
 #include <machine/machine_routines.h>
 #include <pexpert/protos.h>
diff --git a/pexpert/arm/common/pe_serial.c b/pexpert/arm/common/pe_serial.c
index 4b93f24..c4bbadd 100644
--- a/pexpert/arm/common/pe_serial.c
+++ b/pexpert/arm/common/pe_serial.c
@@ -28,6 +28,7 @@
  */
 
 #include <mach/mach_types.h>
+#include <kern/debug.h>
 #include <pexpert/pexpert.h>
 #include <pexpert/arm/boot.h>
 
diff --git a/pexpert/arm/pe_imx53.c b/pexpert/arm/pe_imx53.c
index 5153a3a..f450a6a 100644
--- a/pexpert/arm/pe_imx53.c
+++ b/pexpert/arm/pe_imx53.c
@@ -280,8 +280,8 @@ void imx53_framebuffer_init(void)
     /*
      * This is an emulated framebuffer. 
      */
-    void *framebuffer = pmap_steal_memory(1024 * 768 * 4);
-    void *framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
+    vm_offset_t framebuffer = (vm_offset_t)pmap_steal_memory(1024 * 768 * 4);
+    vm_offset_t framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
 
     uint32_t depth = 4;
     uint32_t width = 1024;
@@ -297,7 +297,7 @@ void imx53_framebuffer_init(void)
     PE_state.video.v_depth = 2 * (8);   // 16bpp
 
     kprintf(KPRINTF_PREFIX "fake framebuffer initialized\n");
-    bzero(framebuffer, (pitch * height));
+    bzero((void *)framebuffer, (pitch * height));
 
     initialize_screen((void *) &PE_state.video, kPETextMode);
 }
diff --git a/pexpert/arm/pe_omap335x.c b/pexpert/arm/pe_omap335x.c
index f65a572..8801492 100644
--- a/pexpert/arm/pe_omap335x.c
+++ b/pexpert/arm/pe_omap335x.c
@@ -349,8 +349,8 @@ void Omap3_framebuffer_init(void)
     /*
      * This is an emulated framebuffer. 
      */
-    void *framebuffer = pmap_steal_memory(1024 * 768 * 4);
-    void *framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
+    vm_offset_t framebuffer = (vm_offset_t)pmap_steal_memory(1024 * 768 * 4);
+    vm_offset_t framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
 
     uint32_t depth = 4;
     uint32_t width = 1024;
@@ -366,7 +366,7 @@ void Omap3_framebuffer_init(void)
     PE_state.video.v_depth = 2 * (8);   // 16bpp
 
     kprintf(KPRINTF_PREFIX "fake framebuffer initialized\n");
-    bzero(framebuffer, (pitch * height));
+    bzero((void *)framebuffer, (pitch * height));
 
     char tempbuf[16];
     initialize_screen((void *) &PE_state.video, kPETextMode);
diff --git a/pexpert/arm/pe_omap3430.c b/pexpert/arm/pe_omap3430.c
index b027578..f61b17c 100644
--- a/pexpert/arm/pe_omap3430.c
+++ b/pexpert/arm/pe_omap3430.c
@@ -474,10 +474,10 @@ void Omap3_framebuffer_init(void)
     /*
      * Allocate framebuffer 
      */
-    void *framebuffer = pmap_steal_memory(lcd_width * lcd_width * 4);
-    void *framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
-    bzero(framebuffer, lcd_width * lcd_height * 4);
-    kprintf(KPRINTF_PREFIX "software framebuffer at %p\n", framebuffer);
+    vm_offset_t framebuffer = (vm_offset_t)pmap_steal_memory(lcd_width * lcd_width * 4);
+    vm_offset_t framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
+    bzero((void *)framebuffer, lcd_width * lcd_height * 4);
+    kprintf(KPRINTF_PREFIX "software framebuffer at %p\n", (void *)framebuffer);
 
 #if 0
     /*
diff --git a/pexpert/arm/pe_omap3530.c b/pexpert/arm/pe_omap3530.c
index 1cc6fbd..44ff6e1 100644
--- a/pexpert/arm/pe_omap3530.c
+++ b/pexpert/arm/pe_omap3530.c
@@ -502,10 +502,10 @@ void Omap3_framebuffer_init(void)
     /*
      * Allocate framebuffer 
      */
-    void *framebuffer = pmap_steal_memory(lcd_width * lcd_width * 4);
-    void *framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
-    bzero(framebuffer, lcd_width * lcd_height * 4);
-    kprintf(KPRINTF_PREFIX "software framebuffer at %p\n", framebuffer);
+    vm_offset_t framebuffer = (vm_offset_t)pmap_steal_memory(lcd_width * lcd_width * 4);
+    vm_offset_t framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
+    bzero((void *)framebuffer, lcd_width * lcd_height * 4);
+    kprintf(KPRINTF_PREFIX "software framebuffer at %p\n", (void *)framebuffer);
 
     /*
      * Set attributes in display controller 
@@ -546,7 +546,7 @@ void Omap3_framebuffer_init(void)
         /*
          * BootX like framebuffer. 
          */
-        memset(framebuffer, 0xb9, lcd_width * lcd_height * 4);
+        memset((void *)framebuffer, 0xb9, lcd_width * lcd_height * 4);
         initialize_screen((void *) &PE_state.video, kPEGraphicsMode);
     } else {
         initialize_screen((void *) &PE_state.video, kPETextMode);
diff --git a/pexpert/arm/pe_realview.c b/pexpert/arm/pe_realview.c
index e822eb3..d0e7627 100644
--- a/pexpert/arm/pe_realview.c
+++ b/pexpert/arm/pe_realview.c
@@ -332,8 +332,8 @@ void RealView_framebuffer_init(void)
      * The hardware demands a framebuffer, but the framebuffer has to be given
      * in a hardware address.
      */
-    void *framebuffer = pmap_steal_memory(1024 * 768 * 4);
-    void *framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
+    vm_offset_t framebuffer = (vm_offset_t)pmap_steal_memory(1024 * 768 * 4);
+    vm_offset_t framebuffer_phys = pmap_extract(kernel_pmap, framebuffer);
 
     uint32_t depth = 2;
     uint32_t width = 1024;
@@ -368,7 +368,7 @@ void RealView_framebuffer_init(void)
     PE_state.video.v_depth = 4 * (8);   // 16bpp
 
     kprintf(KPRINTF_PREFIX "framebuffer initialized\n");
-    bzero(framebuffer, (pitch * height));
+    bzero((void *)framebuffer, (pitch * height));
 
     char tempbuf[16];
     
@@ -376,7 +376,7 @@ void RealView_framebuffer_init(void)
         /*
          * BootX like framebuffer. 
          */
-        memset(framebuffer, 0xb9, PE_state.video.v_rowBytes * PE_state.video.v_height);
+        memset((void *)framebuffer, 0xb9, PE_state.video.v_rowBytes * PE_state.video.v_height);
         initialize_screen((void *) &PE_state.video, kPEGraphicsMode);
     } else {
 		initialize_screen((void *) &PE_state.video, kPETextMode);
diff --git a/pexpert/pexpert/arm/protos.h b/pexpert/pexpert/arm/protos.h
index 61bd7bb..a4f870a 100755
--- a/pexpert/pexpert/arm/protos.h
+++ b/pexpert/pexpert/arm/protos.h
@@ -31,6 +31,7 @@
 #define _PEXPERT_ARM_PROTOS_H
 
 #include <stdint.h>
+#include <mach/vm_types.h>
 
 /*
  * Prototypes used internally by pexpert.
@@ -48,6 +49,7 @@ int serial_getc(void);
 void serial_putc(char);
 void uart_putc(char);
 int uart_getc(void);
+void *pmap_steal_memory(vm_size_t size);
 
 /*
  * Generic 32/16/8-bit memory I/O functions. 
@@ -101,6 +103,8 @@ static inline uint8_t regread8(uint8_t addr)
 void cnputc(char);
 int switch_to_serial_console(void);
 void switch_to_old_console(int);
+boolean_t console_is_serial(void);
+void panic_ui_initialize(const unsigned char *system_clut);
 
 
 /*
diff --git a/pexpert/pexpert/protos.h b/pexpert/pexpert/protos.h
index d544644..f69a2b8 100644
--- a/pexpert/pexpert/protos.h
+++ b/pexpert/pexpert/protos.h
@@ -42,7 +42,7 @@
 
 //------------------------------------------------------------------------
 // from ppc/misc_protos.h
-extern void printf(const char *fmt, ...);
+extern int printf(const char *fmt, ...);
 
 extern void interrupt_enable(void);
 extern void interrupt_disable(void);
